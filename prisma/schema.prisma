generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Attribute {
  id        String   @id @default(cuid())
  name      String   @unique
  category  String
  dataType  String
  description String?
  createdAt DateTime @default(now())
}